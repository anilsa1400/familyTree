{
  "name": "family-tree-platform",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,client -c cyan,magenta \"npm run dev --workspace @family-tree/api\" \"npm run start --workspace @family-tree/client\"",
    "dev:web": "concurrently -n api,web -c cyan,magenta \"npm run dev --workspace @family-tree/api\" \"npm run web --workspace @family-tree/client\"",
    "web": "npm run web --workspace @family-tree/client",
    "dev:api": "npm run dev --workspace @family-tree/api",
    "dev:client": "npm run start --workspace @family-tree/client",
    "db:init": "npm run db:init --workspace @family-tree/api",
    "db:seed": "npm run db:seed --workspace @family-tree/api",
    "build": "npm run build --workspace @family-tree/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
